import 'package:flutter/cupertino.dart';

class SettingScreenss extends StatelessWidget {
  const SettingScreenss({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(

      child: Text('Setting'),
    );
  }
}
